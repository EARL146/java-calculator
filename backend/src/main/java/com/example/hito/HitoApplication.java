package com.example.hito;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * HitoApplication is the entry point of the Spring Boot app.
 * When you run this class, Spring Boot starts the web server on port 8080.
 */
@SpringBootApplication
public class HitoApplication {

    public static void main(String[] args) {
        SpringApplication.run(HitoApplication.class, args);
    }

}
